name: Fifth Workflow
on: [push, issues, pull_request]
# on: workflow_dispatch

jobs:
  run-fifth-command:
    runs-on: ubuntu-latest
    steps:
      - name: Event Vars
        run: |
          echo $GITHUB_SHA
          echo $GITHUB_REF
