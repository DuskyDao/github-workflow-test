name: Fifth Workflow
on:
  issues:
    types: [edited, deleted]
  pull_request:
    types: [assigned, closed]
# on: workflow_dispatch

jobs:
  run-fifth-command:
    runs-on: ubuntu-latest
    steps:
      - name: Event Vars
        run: |
          echo $GITHUB_SHA
          echo $GITHUB_REF
