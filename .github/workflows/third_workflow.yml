name: Third Workflow
on: [push, pull_request]

jobs:
    run-first-command:
        runs-on: ubuntu-latest
        steps:
            - name: Command in Multiline
              run: |
                  pwd
                  ls -a
