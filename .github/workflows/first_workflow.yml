name: First Workflow
# on: [push, pull_request]
on: workflow_dispatch

jobs:
    run-first-command:
        runs-on: ubuntu-latest
        steps:
            - name: Display some Text
              run: echo "Display Hello World"
            - name: Command in Multiline
              run: |
                  node -v
                  npm -v
                  python -V
